# Seed data

This folder contains small, non-sensitive example data to make the project runnable out-of-the-box.

Runtime data (dev/prod) must not be committed to Git and should be provided via volume mounts or an external storage (e.g., S3).
